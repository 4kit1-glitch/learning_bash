#!/bin/bash

# shadowing refers to when a local variable with the same name as a global variable hides the 
# effect of the global variable inside a function
# use the local keyword cause without it variables are global by default